# Copy this file to .env before running `docker compose up --build`.
DISCORD_TOKEN=
# Generate at least 32 random bytes. This must match the website Worker's
# BOT_API_TOKEN secret.
WEB_API_KEY=
ADMIN_DISCORD_USER_ID=

# Optional local overrides.
DATABASE_NAME=KanbanCord
PUBLIC_BOARD_URL=
